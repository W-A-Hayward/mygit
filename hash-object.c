// hash-object  →  write-tree  →  commit-tree  →  log  →  checkout

// given a -w flag (optional, defaults to false), and file path
// determine size

// determine content

// SHA-1 <type> <size>\0<content> to 40 hex string. type = blob | tree | commit

// z-lib compression

// storing using the 2-38 split
